import * as ActionTypes from '../constants';
import {} from 'react/lib/keyMirros'
const Actions = {
    fetchBeers: (payload) => ({
      type: ActionTypes.FETCH_BEERS_REQUEST,
    })
};

export default Actions;